import React from 'react';
import PropTypes from 'prop-types';
import MainBody from '../../components/MainBody';

Home.propTypes = {
    
};

function Home(props) {
    return (
        <main>
            <MainBody />
        </main>
    );
}

export default Home;